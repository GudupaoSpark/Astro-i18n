---
interface Props {
  title?: string;
  lang?: string; // Add lang prop
}

import LanguageSwitcher from '../components/LanguageSwitcher.astro'; // Import LanguageSwitcher component

const { title = 'Astro i18n Demo' } = Astro.props;

// Determine the current language from the URL path
// Assuming the language is the first segment after the base URL, e.g., /en/page or /zh/page
const currentLang = Astro.url.pathname.split('/')[1] || 'en'; // Default to 'en' if not found
---

<!DOCTYPE html>
<html lang={currentLang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <LanguageSwitcher lang={currentLang} />
    <slot />
  </body>
</html>

<style>
  /* 全局樣式 */
  :root {
    --accent: 136, 58, 234;
    --accent-light: 224, 204, 250;
    --accent-dark: 49, 10, 101;
    --accent-gradient: linear-gradient(
      45deg,
      rgb(var(--accent)),
      rgb(var(--accent-light)) 30%,
      white 60%
    );
  }
  html {
    font-family: system-ui, sans-serif;
    background-color: #f6f6f6;
    color: #111;
  }
  body {
    margin: 0;
    padding: 0;
    height: 100vh;
    width: 100%;
  }
  code {
    font-family: Menlo, Monaco, Lucida Console, Liberation Mono, DejaVu Sans Mono,
      Bitstream Vera Sans Mono, Courier New, monospace;
  }
</style>